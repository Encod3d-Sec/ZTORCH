# Approach

The plan board. `offensive.py board` fills the 4a coverage matrix below from the
routing index and this engagement's assets. Work one open `[ ]` row at a time;
mark `[x]` with a `poc/` image as each lands, `[!]` + a Deadends line when a
vector is exhausted.

### 4a. Coverage matrix

| id | asset | vuln class | arsenal | skill | tool | status | poc | poc_kind |
|----|-------|------------|---------|-------|------|--------|-----|----------|
