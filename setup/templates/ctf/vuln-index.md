---
title: "Findings - {{ENGAGEMENT}}"
type: engagement-vuln-index
tags: [engagement, findings, ctf]
date_created: "{{DATE}}"
date_updated: "{{DATE}}"
sources: []
---

# Findings - {{ENGAGEMENT}}

Optional plain findings roll-up for this CTF room. There is no severity/CVSS
bookkeeping here (CTF rooms do not use the FIND severity machinery). The live
attack chain lives in `state.md`'s `## Chain` section (ctf has no `Killchain.md`);
the final boot-to-root recipe lives in
`walkthrough.md` (its TL;DR chain line). This file is only a flat list of the
notable vulns/flags found, for a quick roll-up or a writeup.

| finding | where | how / payload | flag |
|---------|-------|---------------|------|
