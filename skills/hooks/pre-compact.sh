#!/usr/bin/env bash
echo "REMINDER: context is about to compact. Persist state now (via gsd:pause-work): generic/framework summary -> session/hot.md, session/log.md, session/memory.md (NO client specifics). Per-engagement narrative -> targets/<eng>/log.md (audit), git-ignored."
