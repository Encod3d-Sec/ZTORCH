# Wiki pages intentionally NOT surfaced "by context" (excluded from the wiring audit/CI gate).
# One per line:  <slug>  # <reason>
# Only for pages that are navigational/meta/reference, not an attack technique that should
# fire during an engagement. Attack-technique pages should be WIRED, not exempted.
# Maintained by scripts/wiki-wiring-audit.py + subagent proposals. See
# docs/superpowers/specs/2026-07-08-wiki-context-wiring-design.md
active-directory  # AD domain hub / navigational landing page (links all sub-techniques)
active-directory-moc  # Map-of-content / pure navigational index for the AD domain
akams-shortcuts  # aka.ms shortcut link list (cloud/m365 navigation reference), not surfaced by AD recon or prompt context
internal-all-the-things  # Index/landing page for the InternalAllTheThings import collection; navigational
kiosk-escape-and-jail-breakout  # Windows kiosk/jail breakout technique; not AD, not surfaced by AD recon or prompt context
aws-training  # training/lab resource list (flaws.cloud/CloudGoat style), not a detectable attack surface
cloud-moc  # map-of-content navigation hub (auto-generated by build_moc.py), meta page
network-moc  # map-of-content navigational hub; made an anchor in Phase 3, not a by-context attack page
red-team-moc  # map-of-content navigational hub; made an anchor in Phase 3, not a by-context attack page
methodology-moc  # map-of-content navigation hub (auto-generated by build_moc.py)
ctf-creation  # guide for authoring/hosting a BOF CTF, not an offensive technique used during an engagement
miscellaneous-tricks  # catch-all reference of unclassifiable AD/Windows/Linux tricks; no single engagement trigger
pentest-methodology  # pentest process overview; foundational reference surfaced by workflow skills, not a fingerprint
responsible-disclosure  # CVE-disclosure process reference; operationalized by the disclosure workflow skill (outside skills/hunt, so not auditable)
vuln-assessment  # vulnerability-assessment-vs-pentest + CVSS overview; foundational reference
vulnerability-reports  # reporting-phase reference (report structure/templates); not an engagement attack technique
exploit-dev-moc  # map-of-content navigation hub
mobile-iot-moc  # map-of-content navigation hub
osint-moc  # map-of-content navigation hub
linux-moc  # map-of-content navigation hub
cracking-moc  # map-of-content navigation hub
forensics-moc  # map-of-content navigation hub
owasp-top-10  # OWASP Top 10 framework-level index/methodology map over the web pages (meta/reference, not a fireable technique)
web-moc  # Web domain Map-of-Content, auto-generated navigational hub (meta)
bug-bounty-reporting  # reporting-phase reference (report structure), not an engagement attack technique
essential-skills  # foundational skills primer; not surfaced by a specific recon/prompt context
macos-moc  # map-of-content navigational hub for the macos domain (also a direct hunt-macos anchor + playbook ref)
blockchain-moc  # map-of-content navigational hub for the blockchain domain
