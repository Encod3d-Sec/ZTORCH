# Normalize all text files to LF: Windows-seat writes were committing CRLF, which
# bash cannot parse (browser.sh, install-skills.sh broke under WSL bash).
* text=auto eol=lf
*.png -text
*.jpg -text
*.gif -text
*.ico -text
*.pdf -text
